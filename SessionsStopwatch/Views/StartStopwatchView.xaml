<UserControl x:Class="SessionsStopwatch.Views.StartStopwatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SessionsStopwatch.Views"
             mc:Ignorable="d" 
             d:DesignHeight="{StaticResource MeatHeight}" 
             d:DesignWidth="{StaticResource MWWidth}">
    <Grid>
            <Button Style="{StaticResource MeatButton}"
                    Command="{Binding StartStopwatchCommand}">
                <Image Source="/Icons/Play.ico"/>
            </Button>
    </Grid>
</UserControl>
